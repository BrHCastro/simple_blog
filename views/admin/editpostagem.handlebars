{{#each erros}}
    <div class="alert alert-danger">{{text}}</div>
{{else}}
{{/each}}

<h3>Editar Postagem</h3>
<div class="card shadow p-3 mb-5 bg-body rounded">
    <div class="card-body">
        <form action="/admin/postagens/edit" method="POST">
            <input type="hidden" name="id" value="{{posts._id}}">
            <label for="nome">Titulo: </label>
            <input class="form-control" type="text" name="nome" id="nome" placeholder="Título da catagoria" onkeyup="getSlug()" value="{{posts.title}}"/>
            <label for="slug">Slug: </label>
            <input class="form-control disabled" type="text" name="slug" id="slug" placeholder="Slug da categoria" value="{{posts.slug}}"/>
            <br />
            <button type="submit" class="btn btn-warning">Editar</button>
        </form>
    </div>
</div>